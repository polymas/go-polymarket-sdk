# Polymarket SDK 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# ============================================================================
# 必需配置
# ============================================================================

# Web3 私钥（带 0x 前缀）
# 用于签名交易
export POLY_PRIVATE_KEY="0x..."

# API 凭证（从 Polymarket 获取）
# 用于 gasless 交易和 API 认证
export POLY_API_KEY="your-api-key"
export POLY_API_SECRET="your-api-secret"
export POLY_API_PASSPHRASE="your-api-passphrase"

# ============================================================================
# 可选配置
# ============================================================================

# 签名类型（1=Proxy, 2=Safe, 默认=2）
# Proxy: 使用 Polymarket Proxy Wallet
# Safe: 使用 Gnosis Safe Wallet
export POLY_SIGNATURE_TYPE=2

# 测试市场配置（仅用于测试）
# 可以通过市场 slug 或 condition ID 指定测试市场
export POLY_TEST_MARKET_SLUG="btc-updown-4h-1769086800"
# 或者使用 condition ID
# export POLY_TEST_CONDITION_ID="0x..."

# ============================================================================
# 使用说明
# ============================================================================
# 1. 复制此文件: cp .env.example .env
# 2. 编辑 .env 文件，填入你的实际凭证
# 3. 加载环境变量: source .env
# 4. 运行测试: go test ./web3 -run TestSplitAndMergeBTC4H

